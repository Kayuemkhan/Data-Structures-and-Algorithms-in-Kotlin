fun main() {

    var rollno = 55
    val name = "Abdul"
    println(rollno)
    println(name)
     // two types of variable
    //  Immutable using val keyword
    //  Mutable using var keyword
    //  Immutable is also called read-only variables.
    //  Hence, we can not change the value of the variable declared using val keyword.
    // val are not final
}